def solution(name):
    chk = [0 if n == 'A' else 1 for n in name]
    idx_front, idx_back= 0, 0
    front_cnt, back_cnt= 0, 0
    direct = {'front':1, 'back':-1}
    while sum(chk) != 0:
        if chk
    return


print(solution('JAN'))